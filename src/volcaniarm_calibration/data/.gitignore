# Calibration/test run data is tracked (the raw measurements are the
# thesis record; graphs are reproducible from these plus the notebooks).
# Nothing here is ignored anymore; drop a run folder and commit it.
